# Copier en .env.local (jamais commité). Ce fichier s'édite en APPEND-ONLY.
# Base Neon (fournie par l'utilisateur — l'app fonctionne en mode dégradé sans)
DATABASE_URL=

# VPS usine (crons/agents) — identifiants fournis dans un second temps
# VPS_HOST=
# VPS_USER=
